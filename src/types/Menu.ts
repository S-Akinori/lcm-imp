export interface MenuTop {
  titleEn: string
  titleJa: string
  text: string
  href: string
  linkText: string
}

export interface MenuPackage {
  id: string
  title: string
  price: number
  includes: string[]
}